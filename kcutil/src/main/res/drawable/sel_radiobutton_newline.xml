<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 勾选的样式图放到res目录下的drawable中，然后在这里使用 -->

    <item android:drawable="@drawable/ic_new_line" android:state_checked="true" android:color="#2F71FD"/>
    <!-- 未勾选的样式图放到res目录下的drawable中，然后在这里使用 -->
    <item android:drawable="@drawable/touming" android:state_checked="false" android:color="#000000"/>
    <item android:drawable="@drawable/touming" android:color="#000000"/>

</selector>